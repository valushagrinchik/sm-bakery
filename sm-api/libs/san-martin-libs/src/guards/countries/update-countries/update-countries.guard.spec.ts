import { UpdateCountriesGuard } from './update-countries.guard';

describe('UpdateCountriesGuard', () => {
  it('should be defined', () => {
    expect(new UpdateCountriesGuard()).toBeDefined();
  });
});
