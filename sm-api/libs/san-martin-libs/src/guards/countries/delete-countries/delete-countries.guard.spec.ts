import { DeleteCountriesGuard } from './delete-countries.guard';

describe('DeleteCountriesGuard', () => {
  it('should be defined', () => {
    expect(new DeleteCountriesGuard()).toBeDefined();
  });
});
