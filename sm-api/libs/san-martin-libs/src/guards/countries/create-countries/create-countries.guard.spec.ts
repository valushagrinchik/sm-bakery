import { CreateCountriesGuard } from './create-countries.guard';

describe('CreateCountriesGuard', () => {
  it('should be defined', () => {
    expect(new CreateCountriesGuard()).toBeDefined();
  });
});
