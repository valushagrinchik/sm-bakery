import { UpdateVersionsGuard } from './update-versions.guard';

describe('UpdateVersionsGuard', () => {
  it('should be defined', () => {
    expect(new UpdateVersionsGuard()).toBeDefined();
  });
});
