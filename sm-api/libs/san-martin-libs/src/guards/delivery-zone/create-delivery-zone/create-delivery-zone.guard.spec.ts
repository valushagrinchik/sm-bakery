import { CreateDeliveryZoneGuard } from './create-delivery-zone.guard';

describe('CreateDeliveryZoneGuard', () => {
  it('should be defined', () => {
    expect(new CreateDeliveryZoneGuard()).toBeDefined();
  });
});
