import { DeleteDeliveryZoneGuard } from './delete-delivery-zone.guard';

describe('DeleteDeliveryZoneGuard', () => {
  it('should be defined', () => {
    expect(new DeleteDeliveryZoneGuard()).toBeDefined();
  });
});
