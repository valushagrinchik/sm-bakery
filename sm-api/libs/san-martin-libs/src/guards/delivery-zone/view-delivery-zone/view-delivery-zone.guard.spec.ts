import { ViewDeliveryZoneGuard } from './view-delivery-zone.guard';

describe('ViewDeliveryZoneGuard', () => {
  it('should be defined', () => {
    expect(new ViewDeliveryZoneGuard()).toBeDefined();
  });
});
