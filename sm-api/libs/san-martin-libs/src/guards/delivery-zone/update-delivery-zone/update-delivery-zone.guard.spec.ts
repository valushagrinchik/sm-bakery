import { UpdateDeliveryZoneGuard } from './update-delivery-zone.guard';

describe('UpdateDeliveryZoneGuard', () => {
  it('should be defined', () => {
    expect(new UpdateDeliveryZoneGuard()).toBeDefined();
  });
});
