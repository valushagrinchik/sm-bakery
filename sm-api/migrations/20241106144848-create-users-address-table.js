'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.sequelize.query(`CREATE TYPE public.enum_users_address_type AS ENUM
    ('home', 'office', 'other')`);
    await queryInterface.sequelize.query(`CREATE TABLE IF NOT EXISTS public.users_address
(
    id integer NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    user_id integer,
    type enum_users_address_type DEFAULT 'other'::enum_users_address_type,
    country character varying(255) COLLATE pg_catalog."default",
    city character varying(255) COLLATE pg_catalog."default",
    state character varying(255) COLLATE pg_catalog."default",
    sub_locality character varying(255) COLLATE pg_catalog."default",
    address text COLLATE pg_catalog."default",
    position_lat double precision,
    position_lng double precision,
    address_details text COLLATE pg_catalog."default",
    floor_number integer,
    door_number character varying(255) COLLATE pg_catalog."default",
    is_default boolean DEFAULT false,
    created_at timestamp with time zone NOT NULL,
    updated_at timestamp with time zone NOT NULL,
    deleted_at timestamp with time zone,
    CONSTRAINT users_address_pkey PRIMARY KEY (id),
    CONSTRAINT users_address_user_id_fkey FOREIGN KEY (user_id)
        REFERENCES public.users (id) MATCH SIMPLE
        ON UPDATE CASCADE
        ON DELETE CASCADE
)`);
  },

  async down(queryInterface, Sequelize) {
    queryInterface.sequelize.query(`DROP TYPE IF EXISTS public.enum_users_address_type`);
    queryInterface.sequelize.query(`DROP TABLE IF NOT EXISTS public.users_address`);
  },
};
