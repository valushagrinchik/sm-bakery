'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.sequelize.query(`CREATE TABLE IF NOT EXISTS public.modifier_products
(
    id integer NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    modifier_id integer,
    inventory_id character varying(255) COLLATE pg_catalog."default",
    name character varying(255) COLLATE pg_catalog."default",
    sku character varying(255) COLLATE pg_catalog."default",
    type_change character varying(255) COLLATE pg_catalog."default",
    is_default boolean DEFAULT false,
    price numeric(10,2) DEFAULT 0.00,
    created_at timestamp with time zone NOT NULL,
    updated_at timestamp with time zone NOT NULL,
    CONSTRAINT modifier_products_pkey PRIMARY KEY (id),
    CONSTRAINT modifier_products_modifier_id_fkey FOREIGN KEY (modifier_id)
        REFERENCES public.modifiers (id) MATCH SIMPLE
        ON UPDATE CASCADE
        ON DELETE NO ACTION
)`);
  },

  async down(queryInterface, Sequelize) {
    await queryInterface.sequelize.query(`DROP TABLE IF EXISTS public.modifier_products`);
  },
};
