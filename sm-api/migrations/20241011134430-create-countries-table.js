'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.sequelize.query(`DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'enum_countries_status') THEN
        CREATE TYPE public.enum_countries_status AS ENUM
    ('active', 'inactive');
    END IF;
END
$$`);
    await queryInterface.sequelize.query(`CREATE TABLE IF NOT EXISTS public.countries
(
    id integer NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    name character varying(255) COLLATE pg_catalog."default",
    code character varying(255) COLLATE pg_catalog."default",
    phone_code character varying(255) COLLATE pg_catalog."default",
    currency character varying(255) COLLATE pg_catalog."default",
    status enum_countries_status DEFAULT 'inactive'::enum_countries_status,
    created_at timestamp with time zone,
    updated_at timestamp with time zone,
    deleted_at timestamp with time zone,
    CONSTRAINT countries_pkey PRIMARY KEY (id),
    CONSTRAINT countries_code_key UNIQUE (code),
    CONSTRAINT countries_code_key1 UNIQUE (code),
    CONSTRAINT countries_code_key2 UNIQUE (code),
    CONSTRAINT countries_code_key3 UNIQUE (code),
    CONSTRAINT countries_code_key4 UNIQUE (code)
)`);
  },

  async down(queryInterface, Sequelize) {
    await queryInterface.sequelize.query(`DROP TYPE IF EXISTS public.enum_countries_status`);
    await queryInterface.sequelize.query(`DROP TABLE IF EXISTS public.countries`);
  },
};
