'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    queryInterface.sequelize.query(`CREATE TABLE IF NOT EXISTS public.roles
(
    id integer NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    name character varying(255) COLLATE pg_catalog."default",
    created_at timestamp with time zone NOT NULL,
    updated_at timestamp with time zone NOT NULL,
    is_default boolean DEFAULT false,
    customer_app_access boolean DEFAULT false,
    operator_app_access boolean DEFAULT false,
    admin_panel_access boolean DEFAULT false,
    CONSTRAINT roles_pkey PRIMARY KEY (id),
    CONSTRAINT roles_name_key UNIQUE (name),
    CONSTRAINT roles_name_key1 UNIQUE (name),
    CONSTRAINT roles_name_key2 UNIQUE (name),
    CONSTRAINT roles_name_key3 UNIQUE (name),
    CONSTRAINT roles_name_key4 UNIQUE (name)
)`);
  },

  async down(queryInterface, Sequelize) {
    queryInterface.sequelize.query(`DROP TABLE IF EXISTS public.roles`);
  },
};
