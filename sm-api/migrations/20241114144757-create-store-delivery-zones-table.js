'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.sequelize.query(`CREATE TABLE IF NOT EXISTS public.store_delivery_zones
(
    id integer NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    store_id integer,
    delivery_zone_id integer,
    is_main_store boolean DEFAULT false,
    CONSTRAINT store_delivery_zones_pkey PRIMARY KEY (id),
    CONSTRAINT store_delivery_zones_delivery_zone_id_fkey FOREIGN KEY (delivery_zone_id)
        REFERENCES public.delivery_zones (id) MATCH SIMPLE
        ON UPDATE CASCADE
        ON DELETE CASCADE,
    CONSTRAINT store_delivery_zones_store_id_fkey FOREIGN KEY (store_id)
        REFERENCES public.stores (id) MATCH SIMPLE
        ON UPDATE CASCADE
        ON DELETE CASCADE
)`);
  },

  async down(queryInterface, Sequelize) {
    await queryInterface.sequelize.query(`DROP TABLE IF EXISTS public.store_delivery_zones`);
  },
};
