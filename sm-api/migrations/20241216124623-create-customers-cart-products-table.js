'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.sequelize.query(`CREATE TABLE IF NOT EXISTS public.customers_cart_products
(
    id integer NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    cart_id integer,
    product_id integer,
    quantity integer,
    total_price numeric(10,2) DEFAULT 0,
    is_available boolean DEFAULT false,
    CONSTRAINT customers_cart_products_pkey PRIMARY KEY (id),
    CONSTRAINT customers_cart_products_cart_id_fkey FOREIGN KEY (cart_id)
        REFERENCES public.customers_cart (id) MATCH SIMPLE
        ON UPDATE CASCADE
        ON DELETE CASCADE,
    CONSTRAINT customers_cart_products_product_id_fkey FOREIGN KEY (product_id)
        REFERENCES public.products (id) MATCH SIMPLE
        ON UPDATE CASCADE
        ON DELETE CASCADE
)`);
  },

  async down(queryInterface, Sequelize) {
    await queryInterface.sequelize.query(`DROP TABLE IF EXISTS public.customers_cart_products`);
  },
};
